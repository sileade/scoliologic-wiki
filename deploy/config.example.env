# Scoliologic Wiki - Environment Configuration
# Copy this file to config.env and fill in your values

# ============ DOMAIN CONFIGURATION ============
# Your wiki domain (e.g., wiki.scoliologic.ru)
WIKI_DOMAIN=wiki.example.com

# Base domain for Traefik dashboard (only for full stack deployment)
BASE_DOMAIN=example.com

# Email for Let's Encrypt certificates
ACME_EMAIL=admin@example.com

# ============ DATABASE ============
# PostgreSQL credentials
POSTGRES_DB=wiki
POSTGRES_USER=wiki
POSTGRES_PASSWORD=your-secure-password-here

# Full database URL (auto-generated in docker-compose.full.yml)
# For external database, set this directly:
# DATABASE_URL=postgresql://user:password@host:5432/database

# ============ SECURITY ============
# JWT secret for session tokens (generate with: openssl rand -hex 32)
JWT_SECRET=your-jwt-secret-here

# Traefik dashboard auth (generate with: htpasswd -nb admin password)
# Only needed for full stack deployment
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$...

# ============ AUTHENTIK OAUTH ============
# Your Authentik server URL
AUTHENTIK_URL=https://auth.example.com

# OAuth application credentials from Authentik
AUTHENTIK_CLIENT_ID=your-client-id
AUTHENTIK_CLIENT_SECRET=your-client-secret

# ============ S3 STORAGE ============
# S3-compatible storage for media files
S3_ENDPOINT=https://s3.example.com
S3_BUCKET=wiki-media
S3_ACCESS_KEY=your-access-key
S3_SECRET_KEY=your-secret-key
S3_REGION=us-east-1

# ============ AI CONFIGURATION ============
# Ollama URL (default: internal service)
OLLAMA_URL=http://ollama:11434

# Default AI model for text generation
OLLAMA_MODEL=llama3.2

# Embedding model for semantic search
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# ============ OPTIONAL ============
# Redis URL for caching (auto-configured in full stack)
# REDIS_URL=redis://redis:6379

# SMTP for email notifications
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=wiki@example.com
# SMTP_PASSWORD=smtp-password
# SMTP_FROM=wiki@example.com
