# Scoliologic Wiki - Полная документация

## Содержание

1. [Введение](#введение)
2. [Установка и развёртывание](#установка-и-развёртывание)
3. [Основные концепции](#основные-концепции)
4. [Пользовательский интерфейс](#пользовательский-интерфейс)
5. [Редактор и форматирование](#редактор-и-форматирование)
6. [Поиск и AI](#поиск-и-ai)
7. [Управление доступом](#управление-доступом)
8. [Администрирование](#администрирование)
9. [API и интеграции](#api-и-интеграции)
10. [Архитектура](#архитектура)
11. [Решение проблем](#решение-проблем)

---

## Введение

**Scoliologic Wiki** — это современная корпоративная платформа управления знаниями, разработанная для группы компаний Scoliologic. Система объединяет удобство использования Notion с мощью локальных AI-моделей, обеспечивая полную конфиденциальность данных.

### Ключевые особенности

- **Notion-подобный редактор** с полным набором инструментов форматирования
- **Локальный AI** через Ollama для поиска, генерации и анализа текста
- **Интеграция с Authentik** для управления пользователями и группами
- **Полнотекстовый и семантический поиск** с AI-пониманием
- **Версионирование и история** всех изменений
- **Аналитика и метрики** использования
- **Docker Compose** для быстрого развёртывания
- **Тёмная тема** с автоопределением по системным настройкам

---

## Установка и развёртывание

### Требования

- **Docker** и **Docker Compose** (версия 1.29+)
- **8GB+ RAM** (рекомендуется 16GB+ для AI-моделей)
- **20GB+ свободного места** для БД и AI-моделей
- **GPU** (опционально, для ускорения AI-обработки)
- **Authentik-сервер** для OAuth2 аутентификации

### Быстрое развёртывание

#### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/scoliologic/wiki.git
cd wiki
```

#### Шаг 2: Конфигурация окружения

```bash
cp .env.example .env
# Отредактируйте .env с вашими параметрами
nano .env
```

**Основные переменные окружения**:

```env
# Database
DATABASE_URL=postgresql://wiki:password@postgres:5432/wiki

# Authentik OAuth
AUTHENTIK_URL=https://authentik.your-domain.com
AUTHENTIK_TOKEN=your-api-token
AUTHENTIK_CLIENT_ID=your-client-id
AUTHENTIK_CLIENT_SECRET=your-client-secret

# Ollama AI
OLLAMA_URL=http://ollama:11434

# MinIO S3 Storage
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_URL=http://minio:9000

# Application
JWT_SECRET=your-secret-key
VITE_APP_TITLE=Scoliologic Wiki
VITE_APP_LOGO=/logo.png
```

#### Шаг 3: Выбор конфигурации Traefik

**Вариант A: С новым Traefik** (рекомендуется)

```bash
docker-compose -f docker-compose.yml up -d
```

**Вариант B: С существующим Traefik**

```bash
docker-compose -f docker-compose.existing-traefik.yml up -d
```

#### Шаг 4: Инициализация базы данных

```bash
docker-compose exec app pnpm db:push
```

#### Шаг 5: Загрузка AI-моделей

```bash
# Загрузка модели для embeddings
docker-compose exec ollama ollama pull nomic-embed-text

# Загрузка модели для генерации текста
docker-compose exec ollama ollama pull llama3.2
```

#### Шаг 6: Проверка статуса

```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up`.

#### Шаг 7: Доступ к wiki

Откройте в браузере:

```
https://wiki.your-domain.com
```

Или если используется локальное развёртывание:

```
http://localhost:3000
```

### Обновление системы

```bash
# Остановка текущей версии
docker-compose down

# Обновление кода
git pull origin main

# Применение миграций БД
docker-compose exec app pnpm db:push

# Запуск новой версии
docker-compose up -d
```

---

## Основные концепции

### Страницы (Pages)

**Страница** — это основная единица контента в wiki. Каждая страница содержит:

- **Название** — видимое в навигации
- **Содержимое** — отредактированный текст с форматированием
- **Описание** — краткое резюме (опционально)
- **Теги** — для категоризации
- **Статус** — черновик, опубликовано, архивировано
- **Права доступа** — кто может видеть и редактировать

### Иерархия страниц

Страницы организованы в древовидную структуру:

```
Документация
├── API
│   ├── REST API
│   ├── GraphQL API
│   └── WebSocket API
├── Процедуры
│   ├── Развёртывание
│   └── Резервное копирование
└── FAQ
```

### Группы (Groups)

**Группа** — это набор пользователей с одинаковыми правами доступа. Типичные группы:

- **Администраторы** — полный доступ
- **Редакторы** — создание и редактирование
- **Читатели** — только чтение
- **Гости** — доступ только к публичным страницам

### Версии (Versions)

Каждое изменение страницы создаёт новую версию с:

- **Автором** — кто сделал изменение
- **Временем** — когда было сделано
- **Изменениями** — что было изменено
- **Сообщением** — описание изменения (опционально)

---

## Пользовательский интерфейс

### Главная страница

**URL**: `/`

Приветственная страница с:

- Логотипом и названием компании
- Кнопками быстрого доступа (Wiki, Search, Admin)
- Описанием ключевых функций
- Ссылками на документацию

### Страница Wiki

**URL**: `/wiki`

Основной интерфейс для работы с документами:

**Левая панель** — Навигация:
- Иерархия всех страниц
- Кнопка "+" для создания новой страницы
- Поиск по названиям страниц
- Избранные страницы

**Центральная область** — Редактор:
- Название страницы
- Содержимое с форматированием
- Панель инструментов
- История версий

**Правая панель** — AI-помощник:
- Кнопки AI-функций
- Результаты обработки
- Предложения улучшений

### Страница поиска

**URL**: `/search`

Интерфейс для поиска по всей wiki:

- **Поле поиска** — введите запрос
- **Выбор режима** — текстовый или AI-поиск
- **Фильтры** — по дате, автору, категории
- **Результаты** — список найденных страниц

### Админ-панель

**URL**: `/admin` (только для администраторов)

Управление системой:

- **Dashboard** — статистика и метрики
- **Users** — управление пользователями
- **Groups** — управление группами
- **Pages** — управление страницами
- **Analytics** — аналитика использования
- **Settings** — настройки системы

---

## Редактор и форматирование

### Панель инструментов

#### Форматирование текста

| Кнопка | Горячая клавиша | Описание |
|--------|-----------------|---------|
| **B** | `Ctrl+B` | Жирный текст |
| *I* | `Ctrl+I` | Курсивный текст |
| <u>U</u> | `Ctrl+U` | Подчёркнутый текст |
| ~~S~~ | `Ctrl+Shift+X` | Зачёркнутый текст |
| H | `Ctrl+Shift+H` | Выделение цветом |

#### Структурирование

| Кнопка | Горячая клавиша | Описание |
|--------|-----------------|---------|
| H1 | `#` + Space | Заголовок 1 уровня |
| H2 | `##` + Space | Заголовок 2 уровня |
| H3 | `###` + Space | Заголовок 3 уровня |
| • | `-` + Space | Маркированный список |
| 1. | `1.` + Space | Нумерованный список |
| ☑ | `[]` + Space | Список задач |
| > | `>` + Space | Цитата |
| ``` | ` ``` ` | Блок кода |

#### Вставка элементов

| Кнопка | Описание |
|--------|---------|
| 📷 | Вставить изображение из S3 |
| 🎬 | Вставить видео (YouTube, RuTube, VK, S3) |
| 🔗 | Вставить ссылку |
| 📊 | Вставить таблицу |
| 💬 | Вставить цитату |
| </> | Вставить блок кода |

### Markdown сокращения

Система автоматически преобразует Markdown-синтаксис:

```markdown
# Заголовок 1
## Заголовок 2
### Заголовок 3

- Маркированный список
- Второй пункт

1. Нумерованный список
2. Второй пункт

> Цитата

```код```

**Жирный** текст
*Курсивный* текст
~~Зачёркнутый~~ текст
```

### Автоматическое форматирование

Система автоматически форматирует:

- `(c)` → ©
- `(tm)` → ™
- `(r)` → ®
- `->` → →
- `<-` → ←
- `...` → …
- `--` → —
- Кавычки: `"текст"` → "текст"

### Вставка видео

#### YouTube

```
https://www.youtube.com/watch?v=VIDEO_ID
```

#### RuTube

```
https://rutube.ru/video/VIDEO_ID/
```

#### VK Video

```
https://vk.com/video-GROUP_ID_VIDEO_ID
```

#### S3 видео

```
https://s3.your-domain.com/videos/video.mp4
```

---

## Поиск и AI

### Текстовый поиск

**Назначение**: Быстрый поиск по ключевым словам

**Как использовать**:

1. Откройте страницу поиска (`/search`)
2. Введите ключевые слова
3. Выберите режим "Текстовый поиск"
4. Система найдёт все страницы с этими словами

**Примеры**:

- `API документация` — найдёт страницы с обоими словами
- `"REST API"` — найдёт точную фразу
- `документация -старая` — найдёт без слова "старая"

### AI-поиск

**Назначение**: Интеллектуальный поиск по смыслу

**Как использовать**:

1. Откройте страницу поиска
2. Введите вопрос или описание
3. Выберите режим "AI поиск"
4. Система найдёт релевантные страницы по смыслу

**Примеры**:

- `Как настроить аутентификацию?` — найдёт страницы о login/auth
- `Интеграция платёжных систем` — найдёт про платежи
- `Мониторинг и логирование` — найдёт про logs/monitoring

### Фильтры поиска

- **Дата** — с какой даты по какую
- **Автор** — кто создал страницу
- **Статус** — черновик/опубликовано/архив
- **Категория** — по тегам

### AI-помощник

#### Генерация контента

**Функция**: Создание статьи из краткого описания

**Использование**:

1. Откройте новую страницу
2. Нажмите "AI Assistant" → "Generate Content"
3. Введите тему или план
4. Система создаст полную статью

**Пример**:

```
Тема: Настройка двухфакторной аутентификации
↓
Система создаст:
- Введение
- Пошаговые инструкции
- Примеры кода
- Решение проблем
```

#### Расширение текста

**Функция**: Развёртывание кратких заметок в полноценные статьи

**Использование**:

1. Выделите текст
2. Нажмите "AI Assistant" → "Expand"
3. Система развернёт текст с деталями

#### Сокращение текста

**Функция**: Создание краткого резюме длинной статьи

**Использование**:

1. Выделите текст или нажмите на странице
2. Нажмите "AI Assistant" → "Condense"
3. Система создаст краткое резюме

#### Изменение тона

**Функция**: Переделка текста в другом стиле

**Доступные тоны**:

- **Формальный** — официальный стиль для документов
- **Неформальный** — дружелюбный, разговорный тон
- **Технический** — специализированный язык для специалистов

**Использование**:

1. Выделите текст
2. Нажмите "AI Assistant" → "Change Tone"
3. Выберите нужный тон
4. Система переделает текст

#### Проверка грамматики

**Функция**: Автоматическое исправление ошибок

**Использование**:

1. Выделите текст или нажмите на странице
2. Нажмите "AI Assistant" → "Check Grammar"
3. Система найдёт и исправит ошибки

#### Исправление форматирования

**Функция**: Улучшение структуры и оформления

**Использование**:

1. Нажмите "AI Assistant" → "Fix Formatting"
2. Система улучшит:
   - Структуру заголовков
   - Списки и перечисления
   - Отступы и выравнивание
   - Цитаты и выделения

#### Извлечение ключевых слов

**Функция**: Автоматическое определение главных терминов

**Использование**:

1. Нажмите "AI Assistant" → "Extract Keywords"
2. Система найдёт главные термины
3. Они будут добавлены как теги страницы

#### SEO-оптимизация

**Функция**: Рекомендации для лучшей видимости в поиске

**Использование**:

1. Нажмите "AI Assistant" → "SEO Suggestions"
2. Система предложит:
   - Улучшение заголовков
   - Добавление ключевых слов
   - Оптимизацию описания
   - Рекомендации по структуре

#### Создание плана

**Функция**: Генерация структуры статьи из содержания

**Использование**:

1. Нажмите "AI Assistant" → "Create Outline"
2. Система создаст план статьи
3. Вы можете использовать его как основу

---

## Управление доступом

### Роли пользователей

#### Администратор

- Полный доступ ко всем страницам
- Управление пользователями и группами
- Настройка системы
- Просмотр аналитики

#### Редактор

- Создание новых страниц
- Редактирование своих и общих страниц
- Просмотр истории версий
- Управление своим профилем

#### Читатель

- Только чтение назначенных страниц
- Использование поиска
- Просмотр истории версий
- Управление своим профилем

#### Гость

- Доступ только к публичным страницам
- Использование поиска
- Без возможности редактирования

### Уровни доступа к страницам

#### Публичная

- Видна всем пользователям
- Гости могут читать
- Редактирование только авторизованным

#### Для группы

- Видна только членам группы
- Остальные не видят страницу
- Права зависят от роли в группе

#### Приватная

- Видна только владельцу и администраторам
- Остальные не видят страницу
- Полный контроль владельца

### Управление группами

#### Создание группы

1. Откройте админ-панель → Groups
2. Нажмите "Create Group"
3. Введите название и описание
4. Нажмите "Create"

#### Добавление пользователей в группу

1. Откройте группу
2. Нажмите "Add Member"
3. Выберите пользователя
4. Нажмите "Add"

#### Установка прав доступа группе

1. Откройте страницу
2. Нажмите "Share"
3. Выберите группу
4. Установите права (Read/Edit/Admin)
5. Нажмите "Save"

---

## Администрирование

### Админ-панель

**URL**: `/admin`

Доступна только администраторам.

### Dashboard

Показывает:

- Количество пользователей
- Количество страниц
- Количество групп
- Последние действия
- Статистика поиска

### Управление пользователями

#### Просмотр пользователей

1. Откройте Admin → Users
2. Видите список всех пользователей
3. Информация: имя, email, роль, дата создания

#### Изменение роли пользователя

1. Нажмите на пользователя
2. Нажмите "Edit"
3. Измените роль
4. Нажмите "Save"

#### Удаление пользователя

1. Нажмите на пользователя
2. Нажмите "Delete"
3. Подтвердите удаление

### Управление группами

#### Просмотр групп

1. Откройте Admin → Groups
2. Видите список всех групп
3. Информация: название, описание, количество членов

#### Редактирование группы

1. Нажмите на группу
2. Нажмите "Edit"
3. Измените название/описание
4. Нажмите "Save"

#### Удаление группы

1. Нажмите на группу
2. Нажмите "Delete"
3. Подтвердите удаление

### Управление страницами

#### Просмотр всех страниц

1. Откройте Admin → Pages
2. Видите список всех страниц
3. Информация: название, автор, дата создания

#### Удаление страницы

1. Нажмите на страницу
2. Нажмите "Delete"
3. Подтвердите удаление

### Аналитика

#### Просмотр метрик

1. Откройте Admin → Analytics
2. Видите дашборд с:
   - Количество просмотров по страницам
   - Активность пользователей
   - Популярные страницы
   - Топ поисковых запросов

#### Экспорт отчётов

1. Откройте Admin → Analytics
2. Нажмите "Export"
3. Выберите формат (PDF/CSV)
4. Скачайте отчёт

### Настройки системы

#### Основные настройки

1. Откройте Admin → Settings
2. Измените:
   - Название wiki
   - Логотип
   - Описание
   - Язык

#### Интеграция с Authentik

1. Откройте Admin → Settings → Authentik
2. Введите:
   - Authentik URL
   - API Token
   - Client ID
   - Client Secret
3. Нажмите "Save"
4. Система синхронизирует группы

#### Синхронизация групп

1. Откройте Admin → Settings → Sync
2. Нажмите "Sync Now"
3. Система загрузит группы из Authentik

---

## API и интеграции

### REST API

Все операции доступны через REST API на `/api/trpc`.

#### Примеры

**Получение списка страниц**:

```bash
curl -X POST https://wiki.your-domain.com/api/trpc/pages.list \
  -H "Content-Type: application/json" \
  -d '{"input": {}}'
```

**Создание новой страницы**:

```bash
curl -X POST https://wiki.your-domain.com/api/trpc/pages.create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "input": {
      "title": "New Page",
      "content": "Page content",
      "parentId": null
    }
  }'
```

### OAuth2 интеграция

Wiki использует OAuth2 через Authentik. Все пользователи аутентифицируются через ваш Authentik-сервер.

### S3 интеграция

Для загрузки файлов используется S3-совместимое хранилище (MinIO).

**Загрузка файла**:

```javascript
const response = await fetch('/api/trpc/storage.upload', {
  method: 'POST',
  body: formData,
  headers: {
    'Authorization': `Bearer ${token}`
  }
});
```

---

## Архитектура

### Компоненты системы

```
┌─────────────────────────────────────────────────┐
│                   Traefik                        │
│              (Reverse Proxy)                     │
└──────────────────┬──────────────────────────────┘
                   │
        ┌──────────┼──────────┐
        │          │          │
   ┌────▼────┐ ┌──▼────┐ ┌──▼────┐
   │ Frontend │ │ Backend│ │Ollama │
   │ (React)  │ │(Node.js)│ │ (AI) │
   └────┬────┘ └──┬────┘ └──┬────┘
        │         │         │
        └─────────┼─────────┘
                  │
        ┌─────────┼─────────┐
        │         │         │
   ┌────▼────┐ ┌─▼────┐ ┌──▼────┐
   │PostgreSQL│ │MinIO │ │Ollama │
   │ (DB)    │ │(S3)  │ │(LLM)  │
   └─────────┘ └──────┘ └───────┘
```

### Технологический стек

**Frontend**:
- React 19
- TypeScript
- TailwindCSS
- TipTap (редактор)

**Backend**:
- Node.js
- Express.js
- tRPC
- Drizzle ORM

**Database**:
- PostgreSQL
- Redis (кэширование)

**Storage**:
- MinIO (S3)

**AI**:
- Ollama
- Vector embeddings

**Infrastructure**:
- Docker
- Docker Compose
- Traefik

### Поток данных

1. **Пользователь** вводит данные в браузер
2. **Frontend** отправляет запрос на Backend через tRPC
3. **Backend** обрабатывает запрос, проверяет права доступа
4. **Database** сохраняет/получает данные
5. **AI** обрабатывает текст при необходимости
6. **Storage** хранит файлы
7. **Frontend** отображает результат

---

## Решение проблем

### Проблема: Не могу залогиниться

**Решение**:

1. Проверьте, работает ли Authentik-сервер
2. Проверьте конфигурацию OAuth в `.env`
3. Очистите cookies браузера
4. Перезагрузитесь

```bash
# Проверка подключения к Authentik
curl https://your-authentik-url/api/v3/admin/users/
```

### Проблема: AI-функции не работают

**Решение**:

1. Проверьте, работает ли Ollama

```bash
docker-compose logs ollama
```

2. Проверьте, загружены ли модели

```bash
docker-compose exec ollama ollama list
```

3. Загрузите модели если их нет

```bash
docker-compose exec ollama ollama pull llama3.2
docker-compose exec ollama ollama pull nomic-embed-text
```

### Проблема: Медленный поиск

**Решение**:

1. Увеличьте RAM для Ollama в `docker-compose.yml`
2. Используйте GPU для ускорения
3. Проверьте индексы БД

```bash
docker-compose exec postgres psql -U wiki -d wiki -c "REINDEX DATABASE wiki;"
```

### Проблема: Не могу загрузить файл

**Решение**:

1. Проверьте, работает ли MinIO

```bash
docker-compose logs minio
```

2. Проверьте права доступа S3

```bash
docker-compose exec minio mc ls minio/wiki/
```

3. Проверьте размер файла (максимум 100MB)

### Проблема: Ошибка БД

**Решение**:

1. Проверьте логи PostgreSQL

```bash
docker-compose logs postgres
```

2. Проверьте подключение

```bash
docker-compose exec postgres psql -U wiki -d wiki -c "SELECT 1;"
```

3. Пересоздайте БД если нужно

```bash
docker-compose down -v
docker-compose up -d
docker-compose exec app pnpm db:push
```

### Проблема: Высокое использование памяти

**Решение**:

1. Ограничьте память для контейнеров в `docker-compose.yml`
2. Отключите неиспользуемые AI-модели
3. Очистите кэш

```bash
docker-compose exec app pnpm cache clean
```

### Получение логов

```bash
# Логи всех сервисов
docker-compose logs

# Логи конкретного сервиса
docker-compose logs app
docker-compose logs postgres
docker-compose logs ollama
docker-compose logs minio

# Логи в реальном времени
docker-compose logs -f app

# Последние 100 строк
docker-compose logs --tail=100 app
```

---

## Часто задаваемые вопросы

### Как создать резервную копию?

```bash
# Резервная копия БД
docker-compose exec postgres pg_dump -U wiki wiki > backup.sql

# Резервная копия файлов
docker-compose exec minio mc mirror minio/wiki ./wiki-backup/
```

### Как восстановить из резервной копии?

```bash
# Восстановление БД
docker-compose exec -T postgres psql -U wiki wiki < backup.sql

# Восстановление файлов
docker-compose exec minio mc mirror ./wiki-backup/ minio/wiki/
```

### Как обновить систему?

```bash
git pull origin main
docker-compose down
docker-compose up -d
docker-compose exec app pnpm db:push
```

### Как добавить новых пользователей?

Пользователи добавляются автоматически при первом входе через Authentik. Вы можете управлять ими в админ-панели.

### Как экспортировать данные?

```bash
# Экспорт всех страниц в Markdown
docker-compose exec app pnpm export:markdown

# Экспорт в PDF
docker-compose exec app pnpm export:pdf
```

### Как интегрировать с другими системами?

Wiki предоставляет REST API на `/api/trpc`. Вы можете интегрировать с любой системой, которая может отправлять HTTP-запросы.

---

## Поддержка и контакты

Для получения помощи:

1. Проверьте эту документацию
2. Посмотрите логи системы
3. Откройте issue на GitHub
4. Свяжитесь с командой разработки

---

## Лицензия

Scoliologic Wiki разработана для группы компаний Scoliologic.

---

**Версия**: 1.0.0  
**Последнее обновление**: 2026-01-14  
**Автор**: Scoliologic Development Team
